---
name: start-workspace
description: Set up PyAutoLabs workspace or tutorial development — attach or create workspace worktrees, inspect upstream library API changes, and register workspace repos in PyAutoMind.
---

# Start Workspace

Follow [`start_workspace.md`](start_workspace.md) in this directory exactly —
the authoritative workflow body (`reference.md` holds the long-form formats).
Shared context and cross-harness notes: [`../WORKFLOW.md`](../WORKFLOW.md).
Maintain the **library-first rule**: linked workspace work follows the upstream
library PR and must use that PR's API-change summary. Do not duplicate or
reinterpret the workflow here — if it changes, edit `start_workspace.md`.
